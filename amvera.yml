meta:
  environment: python
  toolchain:
    name: pip
    version: "3.10"

build:
  artifacts:
    - requirements.txt
  commands:
    - pip install -r requirements.txt

run:
  command: gunicorn -w 1 -b 0.0.0.0:8080 page_analyzer:app
  persistenceMount: /data
  containerPort: 8080